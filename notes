# How to run program
python scripts/_smoke_test.py     # verify API keys, imports, account connection

python api.py                     # backend REST API on port 8000
cd frontend && npm run dev        # dashboard UI
python run_bot.py                 # main trading loop (paper trading)




# Notes for building
- See if there is a way to add limit buy and sell with offset of either 0.10 or 0.05

- Examine near whole or half dollar trailing stops (maybe need to give more leway when appoaraching)


- Better the filters, link with newsfeed so it auto fills stocks instead of manual entry, more technical indicators, more market data


- MACD, ema 9 20 60, VWAP

- Use logs for analytics


- better the take profit


- change limit ask to +0.10
- time 7-11
- implement the agent learning over trades to get better so it can get a higher percentage win rate over time


no hard settings have the b9t fully autnomous trading on the fly


Strategy B: technical_filters is essentially dead config right now. If you want both running simultaneously, you'd need to either run two separate bot processes (one per strategy) or wire technical_filters
  into a second scan loop in run_bot.py. Want to do that?


  improve entry strategy